(jbuild_version 1)

(executables
 ((names (test))
  (libraries (test_app alcotest cmdliner rresult))))

(alias
 ((name runtest)
  (deps (test.exe app/config.ml app/myocamlbuild.ml app/app.ml))
  (action (run ${<}))))
