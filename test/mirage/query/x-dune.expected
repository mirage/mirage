;; Generated by mirage.%%VERSION%%

(executable
  (name config)
  (flags (:standard -warn-error -A))
  (modules config)
  (libraries mirage))

(rule
  (targets key_gen.ml main.ml)
  (deps config.ml mirage.context)
  (action
    (run ./config.exe build --context-file mirage.context --dry-run)))

(executable
  (public_name noop)
  (name main)
  (package noop)
  (promote (until-clean))
  (libraries lwt mirage-bootvar-unix mirage-clock-unix mirage-logs mirage-runtime mirage-types mirage-unix)
  (link_flags (-thread))
  (modules (:standard \ config))
  (flags -g -w +A-4-41-42-44 -bin-annot -strict-sequence -principal -safe-string)
  (variants unix))
